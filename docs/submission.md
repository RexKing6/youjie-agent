# GOAI 无界应用复赛提交说明

## 作品名称

有界｜制造供应链异常研判与恢复计划智能体

## 一句话介绍

面向制造计划员，基于公开汽车供应链原始行，把邮件/群聊/随机事故转成可追溯影响，使用 LangGraph 编排受限工具、CP-SAT 和独立验证器生成恢复方案，并在人工批准前真实暂停。

## 作品简介

制造异常往往先出现在供应商邮件或业务群聊，影响核算、排产和审批分散在不同工具。有界将其串成安全闭环：语言模型把不可信文本转成带原文证据的候选事件；LangGraph 规划并调用快照、知识检索、影响分析和求解工具；OR-Tools CP-SAT 联合考虑 BOM、库存、供应提前期、有限产能、订单交期与恢复预算；独立验证器从输出 JSON 重查硬约束；图在人工审批处使用 checkpoint + interrupt 暂停，恢复后重新求解并核对场景/计划双 hash，最后只生成不可直接执行的草稿工单。

主演示实际使用 Mendeley CC BY 4.0 汽车供应链 `.xlsb` 原始行。系统校验文件 SHA-256，从第 61 期选取三个重复 BOM 配置，共 217 条逐车需求，聚合后数量仍为 217；每条需求、BOM、库存和容量行均可追溯。客户名称、事故邮件、应急供应商、报价、预算和审批为明确标注的模拟覆盖层。

模拟 Q2J 座椅供应延迟 48 小时后，三种 solver-verified 方案分别按时交付 91/45/0 台，使用应急件 91/45/0，恢复成本 ¥728/¥360/¥0。另有有边界的随机事故 Agent，可按 seed 生成供应延期、供应中断、库存损失、产线停机和需求激增，再走同一验证与人审链路。

## Agent 能力与闭环

1. 任务输入：供应商邮件、群聊、MES 式告警或随机演练事件。
2. 意图理解：结构化 IncidentDraft、实体白名单、原文 span、缺失/冲突检查。
3. 任务规划：LangGraph 按事故类型选择 allowlisted 查询、检索、影响和求解动作。
4. 能力调用：本地知识库、Pydantic、BOM 影响引擎、OR-Tools CP-SAT、独立 verifier。
5. 结果交付：三张结构化方案卡、排程、成本、受影响订单和证据 hash。
6. 验证反馈：67 个测试、90 次真实模型评测、机器可读 artifacts、真实 interrupt、批准/驳回分支。
7. 安全边界：人工审批、双 hash、draft-only；真实接入限于本机 ERPNext 与 OpenMES 测试实例，SAP/金蝶/黑湖仍为合同沙箱，不控制设备。

## 技术与创新

- 公开数据逐行 lineage，而不是只说“参考公开数据”。
- LangGraph 真实 checkpoint/interrupt，而不是页面变量冒充人审。
- solver 之后再用独立代码重放硬约束。
- 批准恢复后重新求解并校验双 hash，防止旧批准和候选篡改。
- 随机事故 Agent 只从合法实体和范围采样，可复现且没有执行权限。
- replay/live 模式明确分开；30/30 replay 合同只作为回归，不声称 live 准确率。
- `qwen3.8-max` 同一 30 条黄金集独立复跑 3 次均为 30/30、越权工具调用 0；原始模型与规则约束后指标分别披露。
- PNG/PDF/CSV 证据由 SHA-256、页码/bbox/行列和新鲜度绑定；新鲜来源冲突时图在求解前暂停。
- 全局无解诊断给出 400 台请求、305 台上界、95 台缺口和 0 工单，而不是伪造排程。
- 提示注入跨过模型边界但没有获得工具、审批或执行权限。
- 三系统对比由仓库内脚本逐项打分，明确限定为架构参考实现，不冒充外部产品排名。

## 数据和许可

- Mendeley 数据：DOI `10.17632/pr3sdy5vp3.1`，CC BY 4.0，行业背景随机化数据。
- 代码和模拟覆盖层：Apache-2.0。
- 不含阿里云内部代码、数据、客户信息或业务规则。
- `lineage.json` 记录原工作表行号、必要转换和模拟字段。

## 当前完成度

- Streamlit Web Demo、CLI、LangGraph、随机演练 Agent：完成。
- 公开数据 hash、217 条原始行和数量守恒：自动验证通过。
- 三方案均为 OPTIMAL 且通过独立 verifier。
- 67 个自动化测试通过；旧 15/15 对抗套件和 30/30 replay 回归继续通过。
- 真实 `qwen3.8-max`：3×30 条全部通过，90 次禁止工具调用 0，平均/p95 延迟 2.14s/2.69s；四个复赛主案例 4/4。
- 固定邮件批准生成 4 张新增 draft-only 动作；既有 committed 供应只引用、不重复建采购单；驳回生成 0 张。
- 来源冲突在 `needs_clarification` 暂停；人工确认 72 小时后才恢复。
- 400 台/24h 返回 `infeasible`：上界 305、缺口 95、工单 0。
- 12 个冻结安全路由案例的三系统比较为 0/12、2/12、12/12；三个实现均重复两次完全一致。
- 恢复驾驶舱已增加订单影响图、三方案 KPI 和同轴甘特图。
- 四种 ERP/MES 集成 profile 各完成 10 次真实 localhost HTTP 链路；当前 72/72 对抗检查通过。真实 ERPNext/OpenMES 实例证据单独验证，不与商业系统合同沙箱混称。HTTP 202 与业务状态分离，ERP成功/MES失败被显示为 `PARTIALLY_APPLIED`；MES产能反馈会作废旧批准、重算并重新等待人审。
- 本机 ERPNext v16 开源测试实例已通过认证 REST 实际创建并 GET 回读 1 张应急 Material Request 与 3 张 Work Order 草稿；既有 217 件 committed 供应只作为计划依据引用，不重复建采购单。写入改变源 revision，重复调用新增 0 张，密钥不进入证据或提交包。回流只监控本次新建单据及其关联 Job Card；本项目不把 ERPNext 冒充独立 MES。
- 本机 OpenMES 开源测试实例固定上游 commit，使用官方 ERP API 接收 3 张 ERPNext 同号工单并回读状态、计划/实际数量和更新时间；人工改变 MES 状态后，回流 revision 变化并使旧 approval 失效。该证据仍只是应用层测试执行，不代表物理设备执行或生产租户认证。
- OpenAPI、AsyncAPI、JSON Schema、交互样例和 SAP S/4+DM / 金蝶+黑湖映射边界已纳入 `contracts/`；均明确为 non-certified/no-live-tenant。
- README、架构、数据溯源、演示脚本、答辩材料和机器可读 artifacts：完成。
- 2 分 02 秒录屏已按当前口径重录：48 小时自然语言事故、真实模型、LangGraph、CP-SAT、人审、ERPNext 草稿写入、OpenMES 同号工单、合同沙箱部分失败与六步证据复盘；190 语速并已烧录中文字幕。
- 复赛 PPT/PDF 已压缩为 8 页主线，移除重复案例和答辩备用内容；可编辑 PPTX 与逐页一致 PDF 均已完成视觉验收。
- 本地复赛环境默认使用真实模型；提交 ZIP 不含密钥。无密钥环境可切换 Replay 完整复现，Live 需部署者通过环境变量或 Streamlit Secrets 配置。
- 当前不把尚未通过外部免登录验收的 Streamlit URL 写成复赛可用地址；复赛附件提供完整本地运行入口。

## 适用边界

作品是隔离的决策辅助 PoC。公开数据不是企业原始流水；随机事故不代表真实风险分布；结果不能证明生产收益。本机 ERPNext 与 OpenMES 都是开源测试实例；前者只创建草稿，后者只导入工单并回读应用状态。SAP/金蝶/黑湖对接仍是项目自建合同沙箱。系统不控制设备，也不替代现场人员决定。
