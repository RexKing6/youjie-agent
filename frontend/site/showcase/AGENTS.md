# Finals showcase

Standalone browser-only entry, Vite build configuration and sanitized recorded fixture. Reuse existing finals components/styles; no simulated live API, no network writes, no browser credential collection. Fixture changes retain source run hash and source commit. Static output goes to root showcase_static; preserve prior release bundles. Build then test navigation and absence of network calls.
